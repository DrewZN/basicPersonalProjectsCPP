1.) Fix the program so that all limbs represent their own respective incorrect guesses
    - If so, change the amount of max guesses to account for the new allowed guesses
        + Also change the "update" functions to reflect this